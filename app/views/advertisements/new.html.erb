<h1>New Ad</h1>

<div class="row">
    <div class="col-md-4">
        <p>Guidelines for ads:</p>
        <ul>
            <li>Tell them they've won something.</li>
            <li>Disguise ads as page content for more clicks.</li>
            <li>Employ bait-and-switch tactics often.</li>
        </ul>
    </div>
    <div class="col-md-8">
        <%= form_for @advertisement do |f| %>
            <div class="form-group">
                <%= f.label :title %>
                <%= f.text_field :title, class: 'form-control', placeholder: "Enter your ad headline" %>
            </div>
            <div class="form-group">
                <%= f.label :copy %>
                <%= f.text_area :copy, rows: 8, class: 'form-control', placeholder: "Enter ad copy here" %>
            </div>
            <div class="form-group">
                <%= f.label :price %>
                <%= f.text_field :price, class: 'form-control', placeholder: "Enter price here" %>
            </div>
            <div class="form-group">
                <%= f.submit "Save", class: 'btn btn-success' %>
            </div>
        <% end %>
    </div>
</div>